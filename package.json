{
  "name": "webar-mindar-three",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist",
    "check:prod": "node scripts/check-prod.mjs",
    "build:prod": "npm run check:prod && vite build",
    "deploy:prod": "npm run build:prod && gh-pages -d dist --dotfiles --message \"Deploy production\"",
    "save:variant": "node scripts/save-variant-from-clipboard.mjs"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^8.0.12",
    "wrangler": "^4.100.0"
  },
  "dependencies": {
    "lil-gui": "^0.21.0",
    "three": "^0.184.0"
  }
}
